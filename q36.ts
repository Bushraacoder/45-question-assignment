function make_shirt(size:string="large", message:string="hello world" ): void {
    console.log(`Creating a ${size} T-shirt with the message: "${message}"`);
}

// Call the function
make_shirt("large", "Hello, World!");