"use strict";
let users = ["john", "Alish", "farrukh", "lily", "Admin"];
if (users.length > 0) {
    console.log("current users");
    for (let user of users) {
        console.log("-" + users);
    }
}
else {
    console.log("we need to find some users.");
}
users = [];
if (users.length > 0) {
    console.log("current users");
    for (let user of users) {
        console.log("-" + users);
    }
}
else {
    console.log("we need to find some users.");
}
